version: 2
project_name: ob

builds:
  - id: windows
    main: ./main.go
    goos:
      - windows
    goarch:
      - amd64
      - arm64
    binary: ob
  - id: unix
    main: ./main.go
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    binary: ob

archives:
  - id: windows-binary
    meta: true
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    builds:
      - windows
  - id: unix-archives
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    builds:
      - unix
    files:
      - none*

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
